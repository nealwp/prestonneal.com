{
  "name": "prestonneal.com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "docker-build": "docker build --tag prestonneal.com:latest .",
    "docker-run": "docker run -d -p 8080:80 --name prestonneal.com prestonneal.com:latest",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  }
}